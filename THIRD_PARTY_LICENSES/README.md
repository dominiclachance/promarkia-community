# Bundled third-party licenses

These notices are generated from the exact installed release environment. They include CPython, OpenSSL, PyInstaller and the Python distributions used to build the desktop application. See `inventory.json` for versions, SPDX expressions, package URLs and source links.
